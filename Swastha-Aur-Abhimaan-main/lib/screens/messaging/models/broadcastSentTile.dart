import 'package:flutter/material.dart';

class BroadcastSentTile extends StatelessWidget {
  const BroadcastSentTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
